public class score {
    
    
    public score(String playerName, int runs) {
       playerName = playerName;
        runs = runs;
    }
    public String getPlayerName() {
        return playerName;
    }
    public void setPlayerName(String playerName) {
        playerName = playerName;
    }
    public int getRuns() {
        return runs;
    }
    public void setRuns(int runs) {
        runs = runs;
    }
    public void display() {
        System.out.println("Player Name: " + playerName + ", Runs: " + runs);
    }

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        ArrayList<score> players = new ArrayList<>();

        System.out.println("Enter the number of players:");
        int numPlayers = scanner.nextInt();
        scanner.nextLine(); 
        scanner sc=new scanner(System.in);
        system.out.println("Enter the name of players"+playerName);
        int n=sc.next Int();
        for (int i=1;i<n;i++);

class player {
    void highscore()
    {
        int max=score[0];
        string topPlayer
    }
    }